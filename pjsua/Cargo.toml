[package]
name = "pjsua"
version = "2.1.0"
edition = "2024"
description = "Rust bindings for pjsua (pjproject SIP library)"
publish = false

[dependencies]

[build-dependencies]
bindgen = "0.71"
pkg-config = "0.3"
cmake = "0.1"
